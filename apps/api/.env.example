NODE_ENV=development
PORT=3000

# Either set DATABASE_URL, or the discrete DB_* vars below (used as fallback).
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/intel_map
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=intel_map
